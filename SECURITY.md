# Security policy

Do not commit credentials, connection strings, API keys, raw protected data, model artifacts, or local database files. Use user secrets or environment variables in later milestones. Administrative routes will require authorization; public research routes will be read-only. Report suspected secret or data exposure privately to the repository owner and rotate affected credentials.
