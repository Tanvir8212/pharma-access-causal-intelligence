# Predictive reproducibility

The manifest records task, dataset and feature-set versions, feature-selection/schema hash, split-manifest and input hashes, seed, trainer and ML.NET package version, .NET version, artifact hash, and synthetic-data marker. Timestamps are operational metadata and never the sole scientific identity.
