# Feature importance

Permutation feature importance uses validation rows, the frozen scoring function and exact ordered feature schema. Repeated deterministic permutations record baseline/permuted metric, deterioration, standard deviation, count and rank. Test labels are prohibited. Correlated predictors can redistribute importance; importance is predictive association, never causality. Row summaries may report values, missingness and supported logistic direction, but tree-model direction is not invented.
