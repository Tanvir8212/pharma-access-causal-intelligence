# Subgroup evaluation

Evaluation dimensions are state, region, launch cohort, observation quarter, quarter since approval, missingness band, historical-volume band and probability band. Each group records support, class counts, prevalence, classification/probability metrics and calibration error. Groups below the configurable minimum or containing one class are explicitly `InsufficientData` and metrics are suppressed. Geographic variation is not described as demographic fairness or discrimination.
