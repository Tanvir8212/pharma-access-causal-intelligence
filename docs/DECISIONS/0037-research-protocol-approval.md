# ADR 0037: Explicit research-protocol approval

Status: Accepted (Milestone 8)

Only an explicitly reviewed protocol with actor, timestamp and reason can govern a ready freeze. Approval is immutable; changes require a new protocol version.
