# ADR 0040: Lock test partition from selection

Status: Accepted (Milestone 8)

Freeze chronological launch-group membership and set `TestPartitionLockedForSelection = true`. Test labels cannot select models, thresholds, calibration or importance.
