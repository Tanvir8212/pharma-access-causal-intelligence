# ADR 0034: Versioned filesystem validation contract

Status: Accepted (Milestone 7)

Exchange inspectable CSV/JSON bundles with strict schemas, immutable run directories, deterministic order, grouped-fold membership, and SHA-256 verification. HTTP and live database exchange are out of scope.
