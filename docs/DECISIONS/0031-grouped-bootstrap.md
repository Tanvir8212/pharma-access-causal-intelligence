# ADR 0031: Bootstrap by generic launch

Status: Accepted (Milestone 6)

Use deterministic percentile bootstrap resampling whole `GenericLaunchId` clusters. Individual row resampling is prohibited when launch clustering is present. Record successful/failed repetitions and require configured minimum success.
