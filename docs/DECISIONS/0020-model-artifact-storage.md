# ADR 0020: Hashed filesystem model artifacts

Status: Accepted (Milestone 4)

Store large model binaries under a configured ignored filesystem root; store lineage metadata in SQL Server. Canonical path validation, immutable version directories, SHA-256 verification, and size limits are mandatory.
