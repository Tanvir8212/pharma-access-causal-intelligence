# Outcome definitions

`next-quarter-state-entry-v1` is binary: true when a not-yet-entered target first satisfies the configured state-entry policy in the immediately next observable quarter; false when that quarter is observed without entry. Censored, suppressed-unknown, or incomplete follow-up is excluded, never converted to false.
