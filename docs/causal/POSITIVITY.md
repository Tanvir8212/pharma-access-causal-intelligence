# Positivity and overlap

Diagnostics include group propensity ranges, common support, rows outside support, treatment prevalence, effective sample size, and extreme weights. Statuses are `Acceptable`, `Warning`, `Failed`, and `InsufficientData`. Failed positivity blocks finalization rather than being hidden by trimming.
