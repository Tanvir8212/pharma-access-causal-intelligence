# Causal reproducibility

The manifest records study/dataset/feature/treatment/outcome/DAG/adjustment versions and hashes, estimand, nuisance and estimator configuration, seeds, grouped-bootstrap configuration, analysis-row hash, diagnostics, estimates, package/.NET versions, commit, timestamps, and synthetic marker. Reports are generated under ignored `artifacts/reports/milestone-6/`.
