# Sensitivity analysis

Structured analyses cover propensity clipping, alternative treatment threshold, adjustment-set version, state-entry policy, eligible-state policy, and placebo timing. Alternatives require separately versioned reruns. Unmeasured-confounding sensitivity is explicitly unsupported until a validated method is implemented; no E-value or Rosenbaum bound is fabricated.
