# Estimate comparison

Statuses are `ExactMatch`, `WithinTolerance`, `DirectionallyConsistent`, `MateriallyDifferent`, `Incomparable`, and `Failed`. Exact comparison requires matching estimand, effect scale, target population, exclusions, and nuisance configuration. Independent estimates need not be identical. Different estimands or scales are `Incomparable`.
