# DoWhy validation

DoWhy consumes only the exported synthetic cohort and human-authored graph, identifies a backdoor estimand without changing common causes, and estimates a compatible regression effect. Identification means the graph implies an estimand; it does not establish that the graph or exchangeability assumptions are true.
