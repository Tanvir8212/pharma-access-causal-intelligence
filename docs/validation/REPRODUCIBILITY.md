# Milestone 7 reproducibility

Every run records contract/run/study/dataset/feature/DAG/adjustment/treatment/outcome versions, estimand, scale, window, counts/exclusions, columns, null/encoding rules, seeds, precision policy, source commit, interpreter/packages, grouped folds, SHA-256 file hashes, and a reproducibility hash. Outputs are deterministic synthetic development artifacts; declared timestamps are excluded from equality expectations.
