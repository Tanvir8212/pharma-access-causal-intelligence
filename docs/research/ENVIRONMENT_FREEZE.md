# Environment freeze

Record OS, .NET SDK/target frameworks, NuGet versions, Python version and lock hash, DoWhy, EconML, pandas, NumPy, scikit-learn, ML.NET, EF SQL Server provider, commit and dirty status. Do not export usernames, tokens or sensitive absolute paths. Dirty status is a warning for synthetic development and requires an explicit policy decision before a real freeze.
