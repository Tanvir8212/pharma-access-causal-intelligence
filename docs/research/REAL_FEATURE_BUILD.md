# Real feature build

Feature generation begins only after a distinct real dataset reaches `Validated` through successful reconciliation and mapping review. It uses the approved state-entry, eligibility, historical-weight, missingness, suppression, and censoring definitions. Generated schema and deterministic hash must match the frozen protocol; blocking leakage or schema mismatch stops execution. This milestone does not train models.
