# Freeze approval

Protocol and freeze approvals are distinct, explicit audited decisions. Each requires actor, UTC time and reason. A freeze requires an approved protocol and zero blocking findings. Approval makes lineage immutable. Replacements are new versions. No anonymous/public approval endpoint exists. Synthetic approval proves workflow mechanics only.
