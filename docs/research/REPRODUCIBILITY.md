# Reproducibility

Future experiments must record commit, dataset and feature versions, configuration, seed, .NET/Python/package versions, split periods, and artifact hashes in a research manifest. Milestone 0 pins the installed SDK through `global.json` and centralizes its necessary test package versions.
