# Real split execution

The approved temporal grouped split assigns complete `GenericLaunchId` groups chronologically. Exact memberships and the split hash are exported. Launch overlap is blocking. The test partition is locked and its outcomes are unavailable to selection code. Milestone 9 prepares this execution but trains no candidate and selects no model.
